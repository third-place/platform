apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.name }}
spec:
  selector:
    app: {{ .Release.name }}
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.port }}
      protocol: TCP
      targetPort: {{ .Values.targetPort }}